# V92 core context — all-instance completion gate

## Mission

V92 is authorized to study one question only:

```text
Can the low-width remote-point algorithm be completed by a high-width win
branch so that every input receives a theorem-native canonical output?
```

No generic width census, new certificate ladder, or citation-only meta-complexity survey counts as progress.

## Required input/output contract

V92 must select one exact circuit representation and define a total procedure with two internally checkable branches:

1. **low width:** construct or verify a decomposition with `k=O(sqrt(log m))` and invoke the inherited algorithm;
2. **high width:** use the obstruction certificate to return an avoided string in the same canonical/single-valued model.

The two branches must agree on one output specification. Merely deciding which branch holds is insufficient.

## Promotion outcomes

V92 is promotable only with one of:

- a proved high-width-to-avoidance lemma completing the total algorithm;
- a reduction from the high-width case to a checked still-open theorem with all parameters filled;
- a rigorous obstruction showing that the selected high-width certificate cannot determine a single-valued avoided output, followed by closure of that certificate choice.

## Barrier gate

Before using Missing-String communication complexity, state whether the proposed step algebrizes in the Chen–Hu–Ren sense. If it does, V92 must explain why the target theorem is outside the barrier; otherwise that route is rejected before experimentation.

## Stop rule

If no candidate high-width branch produces theorem-native output, close the all-instance-completion attempt and move to a different target class rather than optimizing the low-width runtime.
