# V112 scientific status

## Established inside the candidate package

- Exact recognition of the `k=2` serial two-lobe MUX support template in linear time.
- Exact decision/construction for the lobe-disjoint phase-transfer certificate class using only constant-size local choices per shared hub.
- Sound lifting of a compatible pair into one missing full output word.
- Infinite exact-stretch periodic signed family with `n=5(d+1)`, `m=n+1`, and minimum overlap `d`.
- The same optimum-flow face contains both a compatible and an incompatible minimum-overlap pair for every depth.
- Hall minimality of the serial support for all depths.
- Exact V102 strong-affine-backdoor size `beta=3(d+1)=3n/5` on the periodic family.
- Finite audit that the deterministic V111 reference implementation rejects the periodic family through depth 50.
- Primary and independent verifier suites included.

## Still open

- Target compatibility over all minimum-cost flow decompositions in an arbitrary MUX return network.
- Whether the general compatibility-selection problem is polynomial under the exact MUX transition structure or contains a hardness core.
- All essential MUX/bijunctive `0x1b` circuits.
- Unrestricted `NC0_3-Avoid`.
- Any new general circuit lower bound.
- P versus NP.

## Validation status

No novelty, priority, or external peer-review claim is made.  V112 is a structured-class theorem and a counterexample to overlap-optimality invariance, not an all-MUX theorem.
