# Frozen context for Laboratory V68

## Stable facts

1. For disjoint affine-cell systems, `c` multiplies and a tree can be composed by grafting.
2. Direct sums of V57 components have `c=1` and an additive leaf upper bound; this route cannot amplify consistent branches.
3. Two explicit cyclic overlap families have `c=1` for every tested `4<=n<=12`.
4. A seed-42, 4,000-system positive-fiber `0x07` probe preserves `c=16` at `n=10` and `c=36` at `n=11`.
5. The `c=36` witness has `L_aff=61`, `L_greedy=62`, and selected-policy `G_aff=108`.
6. Finite growth does not establish exponential growth, a polynomial bound, or unrestricted `NC0_3-Avoid`.
7. The direct P-versus-NP route remains inactive.
8. Earliest external follow-up date remains 2026-08-24.

## Required V68 behavior

1. Analyze the `c=36` witness by variable incidence position, support overlap, partition type, and conditional factorization.
2. Attempt to turn the observed mechanism into an explicit recursive or concatenated family.
3. Search separately for lower bounds on tree leaves and upper bounds on merged DAG states.
4. Scale with the sandwich `c <= L_aff <= L_greedy`; do not compute exact `L_aff` indiscriminately.
5. Extend the probe beyond positive-fiber `0x07` to all six non-affine classes and parity/support-expansion mixtures.
6. Preserve the smallest witness for every new record and use an independent brute-force checker.
7. Do not infer asymptotic behavior from sampled maxima.
8. Merge only after quick and full CI pass.
