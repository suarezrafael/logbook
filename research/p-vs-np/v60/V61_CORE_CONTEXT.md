# Frozen context for Laboratory V61

## Stable facts

1. The active product is an `NC0_k-Avoid` manuscript, not a claimed route to P versus NP.
2. V53's girth and logarithmic-syndrome implications remain retracted.
3. V56 solves efficiently represented affine-fiber mixtures at `m>n`.
4. V57 supplies a finite and infinite bijunctive irredundancy barrier.
5. V58 defines orientation depth and proves an `m^{O(d)} poly(n+m)` algorithm.
6. The one-flip exact search is complete through `n=8`; `n=9` is incomplete and deprioritized.
7. V59 proves isoperimetric boundary abundance and refutes three strict-improvement potentials.
8. V60 proves that polynomial-time image membership plus `m>n` gives Las Vegas avoidance in expected at most two trials.
9. `STATE.md`, `LEDGER.json` and `verify_all.sh` are the stable repository entry points.
10. External contact remains unsent.

## Primary objective

Turn the V54–V60 result chain into a concise externally reviewable manuscript draft.

## Required tasks

1. Write a unified notation layer independent of version numbers.
2. Extract the exact theorem/proof chain for V54, V56, V57, V58 and V59.
3. Recheck every citation and novelty statement against primary literature.
4. Move computational tables and exhaustive classifications to appendices or supplements.
5. Run `verify_all.sh --full` in a complete checkout and preserve the output.
6. Prepare one reviewer packet with theorem statements, proofs, counterexamples and reproduction commands.
7. Update external-contact status only after explicit authorization.

## Non-goals

- do not resume `n=9` brute force merely to extend a finite sequence;
- do not claim that deterministic derandomization here implies unrestricted lower bounds;
- do not create new progress scores toward P versus NP;
- do not merge the draft PR into `main` without an explicit owner decision.

## Promotion criteria

V61 may be promoted if it delivers at least one of:

- a coherent manuscript draft whose statements match the ledger;
- a documented external mathematical review with resolved corrections;
- a primary-source prior-art report that materially changes novelty claims;
- a clean full-verification transcript and reproducible review archive.
