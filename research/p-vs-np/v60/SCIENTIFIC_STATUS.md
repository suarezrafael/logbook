# Scientific status — V60

## Promoted

1. The elementary Las Vegas theorem for polynomial-time image membership and `m>n`.
2. The exact expected-trial bound `1/(1-2^(n-m))`, including possible equality `2` at stretch one.
3. The cumulative `STATE.md` and `LEDGER.json` as the authoritative orientation layer.
4. The program decision to treat the current result chain as an `NC0_k-Avoid` research product.
5. The demotion of complete `n=9` enumeration from primary goal to falsification/regression work.

## Preserved from earlier versions

- V53 retractions and regression examples;
- V54 pure-`AND_k` separator;
- V55/V56 affine classification and algorithm;
- V57 bijunctive irredundancy barrier;
- V58 orientation depth and FPT algorithm;
- V59 isoperimetric theorem and flat-potential barrier.

## Not promoted

- a deterministic polynomial-time algorithm for general `NC0_3-Avoid`;
- any universal walking potential;
- completion of exact search at `n=9`;
- SAT Modulo Symmetries or DRAT implementation;
- unrestricted circuit lower bounds;
- a reduction connecting the current easy-membership regime to P versus NP;
- novelty or priority.

## External-review status

No external contact was sent in V60. Drafts remain drafts until explicit authorization and a sent-status record exist.

## Promotion decision

**V60 passes as a consolidation and regime-separation laboratory.** Its value is in correcting project direction, reducing repository orientation cost and defining a reviewable manuscript target. It is not promoted as a breakthrough complexity result.
