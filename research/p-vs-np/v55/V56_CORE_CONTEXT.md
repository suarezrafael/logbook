# Core context for Laboratory V56

## Stable facts after V55

1. The V53 girth implication and `Omega(log n)` family remain retracted.
2. V54 gives a degree-at-most-four separator for pure `AND3` positive stretch.
3. The 256 ternary functions form 14 NPN classes.
4. Eight classes have an affine output orientation.
5. Four affine classes are essential: `01`, `06`, `18`, `69`.
6. Any mixture of affine-fiber gates is avoidable in polynomial time for `m>n+1` by block-subspace redundancy.
7. The antipodal-pair orbit `18` is avoidable at stretch one `m>n` because all lifted rows lie in an `n`-dimensional even-parity augmented space.
8. The parity orbit `69` is avoidable at stretch one by affine output rank.
9. The distance-two pair orbit `06` is currently solved only for `m>n+1` by this method.
10. The six essential non-affine classes are `07`, `16`, `17`, `19`, `1b`, and `1e`.
11. General stretch-one `NC0_3-Avoid` remains open.
12. Novelty and prior art for the block theorem are not externally confirmed.

## Mandatory focus

Move from affine fibers to finite unions of affine states.

## Priority routes

1. Compute minimal affine covers of both fibers of the six non-affine classes.
2. Define a gate state as one cell of an affine cover and build block implications between states.
3. Test whether one output value can be forced by boundedly many affine states in every positive-stretch support structure.
4. Prioritize `0x17`, the signed-majority orbit, and connect it to earlier MAJ3 motif laboratories.
5. Determine whether the extra output in the `0x06` theorem can be removed.
6. Search exhaustively for stretch-one frontier circuits with no certificate in the proposed state system.
7. Compare with Schaefer affine/bijunctive/Horn relations, polymorphisms, and implication hypergraphs.
8. Ask GGNS/Kuntewar–Sarma about prior art only with the V53 correction and V55 theorem stated precisely.

## Promotion criteria

- stretch-one algorithm for one of the six non-affine essential classes;
- removal of the extra output for `0x06`;
- finite-state core theorem covering mixtures of at least two essential classes;
- minimal counterexample proving a proposed state system insufficient;
- specialist prior-art confirmation or correction.
